INTERNAL_API_SECRET=

# SECURITY WARNING: don't run with debug turned on in production!
DEBUG=True

# Hosts allowed to access the service (comma-separated list)
ALLOWED_HOSTS=localhost,127.0.0.1

# --- Database Settings (Example using SQLite for dev) ---



# --- Redis Settings (for caching, quotas, rate limiting) ---
# URL for the Redis instance used by the template service
REDIS_URL=redis://localhost:6379/2 # Use a different DB number for isolation if needed

# --- Prometheus Metrics ---
# Enable/disable Prometheus metrics collection
METRICS_ENABLED=True

# --- Template Cache Settings ---
# Enable/disable template caching
TEMPLATE_CACHE_ENABLED=True
# Key name for the template cache version in Redis
TEMPLATE_CACHE_VERSION_KEY=template_cache_version
# Interval (in seconds) for background cache synchronization (if implemented)
TEMPLATE_CACHE_SYNC_INTERVAL=300


DB_NAME=gateway_db
DB_USER=postgres postgres 
DB_PASSWORD=
DB_HOST=
DB_PORT=5432  
DATABASE_URL= 